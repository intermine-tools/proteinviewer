
  describe('neeps', function() {
    it('tatties', function() {
      var b = require('../js/steps-protein-viewer');

      // just checking that _ works
      expect([1, 2, 3].length).toEqual(3);
    });
    b.init();
  });
